import EVENT_UTILITIES from './modules/event-utility-helpers';

global.Helpers = EVENT_UTILITIES;

// var arr = [
//   { util: EVENT_UTILITIES },
// ];

// global.Helpers = arr.reduce(function (result, item) {
//   var key = Object.keys(item);
//   result[key] = item[key];
//   return result;
// }, {});